#/usr/bin/env bash

#$HOME = user home directory
#$PATH = directories where commands are searched
#$HOSTNAME = hostname of the macine
#$SHELL = shell being used
#$USER = user of this session
#$TERM = type of terminal used

echo "Ayo have some envs"
echo "$HOSTNAME"
echo "$USER"
echo "$HOME"
exit 0
